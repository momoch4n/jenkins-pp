pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'another jenkins pp'
                sh 'echo Integrating Jenkins PP with GitHub webhook using Jenkinsfile'
            }
        }
    }
}
